services:
  db:
    ports: ["5432:5432"]
  redis:
    ports: ["6379:6379"]
  backend:
    volumes:
      - ./backend/app:/app/app:ro
    ports: ["8000:8000"]
  worker:
    volumes:
      - ./backend/app:/app/app:ro
  frontend:
    ports: ["3000:3000"]
