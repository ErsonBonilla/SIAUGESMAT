# Dependencias de desarrollo (linting, verificación de código muerto y testing).
# Instalación: pip install -r requirements-dev.txt
-r requirements.txt

ruff==0.16.1
vulture>=2.7
pytest>=8.0.0
pytest-asyncio>=0.23.0
